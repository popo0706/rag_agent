# ===============================================================
# 【要約】
# .env ファイルに書いてある各種 API キー（例：LANGCHAIN_API_KEY など）を
# プログラム起動時に自動的に環境変数へ読み込ませる最小サンプルです。
# これにより、ソースコード中でキー文字列を直書きせずに安全に利用できます。
# ===============================================================

"""
【例】
# client = Client()      # api_key 引数なし → LANGCHAIN_API_KEY を自動使用
#
# 上記は “クライアント（例：LangChain の Client クラス）を
#  api_key 未指定で生成した場合、環境変数 LANGCHAIN_API_KEY が
#  自動的に参照される” という動作例を示しています。
"""

from dotenv import load_dotenv  # .env ファイルを読み込むヘルパーライブラリ
import os  # os: 環境変数を扱う標準ライブラリ

load_dotenv()  # .env に書かれた内容を環境変数へ取り込む
